{% extends 'xgsb/bo/layout.html.twig' %}

{% block title %}Modifier {{ menu.name }} - XGSB Backoffice{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1>Edition {{ menu.name }}</h1>
        </div>
        <div class="card-body">
            {{ include('xgsb/bo/menu/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>
        <div class="card-footer">
            <a href="{{ path('xgsb_bo_menu_index') }}" class="btn btn-dark">
                <span class="icon-chevron-left"></span> Retour à la liste
            </a>
            {{ include('xgsb/bo/menu/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
