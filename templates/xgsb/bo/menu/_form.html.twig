{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary" type="submit">
        <span class="icon-floppy-disk"></span> {{ button_label|default('Enregistrer') }}
    </button>
{{ form_end(form) }}
