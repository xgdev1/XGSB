{% extends 'base.html.twig' %}

{% block title %}Menu{% endblock %}

{% block body %}
    <h1>Menu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ menu.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_x_g_s_b_b_o_menu_index') }}">back to list</a>

    <a href="{{ path('app_x_g_s_b_b_o_menu_edit', {'id': menu.id}) }}">edit</a>

    {{ include('xgsb/bo/menu/_delete_form.html.twig') }}
{% endblock %}
