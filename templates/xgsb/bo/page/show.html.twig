{% extends 'base.html.twig' %}

{% block title %}Page{% endblock %}

{% block body %}
    <h1>Page</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ page.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ page.Name }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ page.Title }}</td>
            </tr>
            <tr>
                <th>Ordre</th>
                <td>{{ page.Ordre }}</td>
            </tr>
            <tr>
                <th>Parameters</th>
                <td>{{ page.parameters ? page.parameters|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ page.DateCreation ? page.DateCreation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_x_g_s_b_b_o_page_index') }}">back to list</a>

    <a href="{{ path('app_x_g_s_b_b_o_page_edit', {'id': page.id}) }}">edit</a>

    {{ include('xgsb/bo/page/_delete_form.html.twig') }}
{% endblock %}
